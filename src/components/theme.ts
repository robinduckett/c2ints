import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    background: {
      default: '#000000'
    },
    mode: 'dark',
    primary: {
      main: '#2e7d32',
    },
    secondary: {
      main: '#b9f6ca',
    },
  },
});

export default theme;
